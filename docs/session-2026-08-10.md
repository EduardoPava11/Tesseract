# Session record — 2026-08-10 (sunset)

*One day, one law, applied three times: nothing perceptual may be a
naked constant. Everything below is committed in one batch; sim
160/160, spec 36/36 (12 files enrolled today at zero), grid lint
clean. Device evidence: tesseract_1786415055.gif — varied binomial
background, warm comp-halo at the silhouette, no solid fill, no seam.*

## Arc 1 — the constant-free role law (morning)

Daniel: "we should never leave naked constant like this." The DYAD
trichotomy (faceThreshold 0.6, bleedWidth 0.15, bleedGamma 0.5 — the
"~46 cm" background) was replaced by the two-phase tied-variance
mixture fit on the capture's own pooled depth field
(spec/temporal/DepthMixture.hs DM1–DM12, DepthMixture.swift):
t(s) = logistic((s*−s)/τ), crossover = equal free energy, band width
= τ·ln 81 emergent, trichotomy = the Bayer coverage extrema
{1/32, 31/32}. Rulings: R1 stats weights = 1−t; R2 EMA α = derived
Kalman gain (localLevelAlpha, Muth 1960); R3 BIC single-phase ⇒
all-face. m* is REPORTED in meters in the DYAD MIXTURE provenance —
an output, never an input.

## Arc 2 — form follows function (afternoon)

The atom IS the GIF pixel; growth = more atoms, never a bigger atom.
Buttons upscaled (shutter 22 cells, action rows 13); later the whole
TypeRows registry (micro 4 / label 7 / body 9 / counter 11 /
display 16). Tech-debt excision: the unreachable A/B–gene–elite arc
(~22 files incl. never-compiling MLX code), the dormant GPU quantize
path (the raw-meters landmine), channel thumbs, dead GridLayout
claims. Submission: PrivacyInfo required-reason APIs (UserDefaults /
CA92.1 — ITMS-91053 closed), ITSAppUsesNonExemptEncryption NO, the 4
latent AxiomTests + the Dissonance seam test fixed (stale pre-pivot
fixtures — including a zero-mass far half that lawfully silenced its
own assertion). LatticeLawTests puts the three selfChecks in CI.

## Arc 3 — the aerial mirror (evening → night)

Daniel rejected the solid background again: "go back to the binomial
distribution." v4 (spec §6 v4): far = σ-mirror of the pixel's OWN
nearest primary — background occupies the mirrored binomial shells.
Then the depth↔color workflow (7 agents, 3 lenses, adversarial judge;
docs/depth-color-scales.md) synthesized THE AERIAL MIRROR LAW:

    σ(s) · γ(s) = σ_base(K),   γ(s) = 1/(2−s)

— a pixel buys its temporal octave with a chroma octave; aerial
perspective as a conservation law, zero new constants. Spec phase
(DY9–DY12: exact-rational invariant, seam-death, aerial occupancy,
stats-on-ŷ; DM11/DM12: the rung-16 τ-lift identity) and the
Swift+Metal port landed the same night, comp-halo default: DyadPipeline
γ-stages every pixel + solves stats on ŷ (ANE untouched); DyadPreview
= τ-lifted rung-16 fit at 5 Hz + γ-staged assignment at 20 Hz — on the
GPU via the new aerialPreview kernel (meters→signal in-kernel, anchors
from DepthSignal.swift; CPU reference fallback; fp32 near-ties only).
FACE mode joined the unified preview.

Also: palette-creation visibility (per-frame tables published during
processing → CellPalette swatch), the GIF LIBRARY (auto-archive to
Documents/Library; tiles → playing GIF + 256-entry palette + role-law
provenance, all read from the GIF's own bytes), preview/export
unification at 20/10/5 on the TL8/TL9 ladder.

## Open at sunset

- **R4**: comp-halo (shipped) vs faithful-hue σ side — DY11 measured
  faithful as aggregate-closer to ŷ; decide on Mac renders + device
  feel; localized swap.
- **R5**: PerfectQuantizer's naked constants (tesseract/refined path).
- **R6**: ratify γ (per-pixel geometry) vs any future global
  chroma-gain slot as distinct.
- Workflow steps 4/5/8/9/10: Mac occupancy census, synthetic
  pushforward harness, mirror-histogram provenance, the byte gate,
  the full device pass.
- Known from before: first thorough FACE device run; FRONT-RAW probe;
  privacy-policy URL hosting; App Store Connect metadata.
