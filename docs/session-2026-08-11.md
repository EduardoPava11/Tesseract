# Session record — 2026-08-10/11: the phase ruling, the loop, the blue face

The day the palette went from aesthetics to physics, in four rulings
and one device capture that ended the night with its own diagnosis.

## 1. The Wada detour (superseded same day, demoted to prior)

Daniel: improve the background — "a blue light layer atop"; north
star Sanzo Wada. Diagnosis: v4's comp preserved L exactly and
gamut-maxed chroma — skin's 180° partner is always same-lightness
max-chroma cyan. The dictionary (348 combos digitized,
scripts/wada/) measured in OKLab: hue interval near-uniform (σ =
hue+180° survives), chroma power law lnC_G = −1.318 + 0.747·lnC_S,
ground L ⊥ figure L at 0.617. Landed as v5-W (spec DY13–15, ports,
fixtures, python gate 8/8). Then Daniel's second ruling superseded
the framing; the Wada FAMILY survives with per-capture moments
(§3), the dictionary constants as the single-phase PRIOR.

## 2. THE PHASE RULING → research workflow → PhasePalette

"Background goes to CHAOS, the user stays in ORDER, boundary layer;
the 256 colors best represent the scene at 16/32/64 reads; back to
information theory." An 11-agent workflow (repo grounding ×3,
literature ×4 — Rose deterministic annealing, Zador/Gersho,
Jaynes/dither, interfaces — synthesis, 3 adversarial verifiers)
produced docs/phase-palette-design.md with every verifier finding
dispositioned. Keystone PP1: D16+D32+D64 = (3‖u4‖²+2‖u2‖²+‖u1‖²)/N
— one derived kernel, 3:2:1 exchange rate. Rulings R1–R5 confirmed.
spec/quantization/PhasePalette.hs PP1–PP16 all green.

## 3. Steps 2–3: telemetry + the scene-fit ground law

- PhaseTelemetry.swift: D_K + Haar bands + PP1 gap witness + Σ log W
  chaos bill → "PHASE F v1" line in the DYAD provenance comment.
- Scene fit (R2): GroundMoments moment-matched per capture (βC = sd
  ratio of lnC, αC from means, ΔL from background mean L; staged
  labs, σ weights, stats-α EMA; uncapped). Wada prior on the
  single-phase path (capped). Trace DYAD STATS v2 (13/frame);
  rebuildTables reads v1 and v2. DyadPreview unified. DyadAssign
  ANE untouched.

## 4. ANE LOOPS (ruling: "loops. we need ANE loops.")

- L1 spec/neural/ANELoop.hs AL1–AL8: M = I+Π₂+Π₄ diagonal in bands
  (eigenvalues 3:2:1), F block-exact over 4³ spacetime blocks,
  monotone Gauss–Seidel exchange with curvature 1+1/8+1/64, clock
  identity 20/5 = 4. Suite 38/38.
- L2 nn/ane-loop/build_model.py → ANELoopModel.mlpackage: 4096
  blocks × K=4 × 64 stages fused. Model F = 5308.41 vs float64
  5308.40; 99.83% assignments; 32 ms/dispatch (M3 Max) = 0.13 ms
  per fused stage. Graph is PHASE-AGNOSTIC — the offset
  architecture is the blocking feed.
- Offsets (design §5½): recommended B+E — temporal polyphase
  (rung-16 output every frame after 4-frame warmup) + streaming
  warm starts (~1 sweep/frame). Rulings open: D (overlapped
  objective), F (per-channel rungs).
- L3 ANELoop.swift: CPU twin (the law) + engine wrapper +
  refineFarBlocks flag-gated behind CameraConfig.phaseChaosLoop
  (default OFF). Fully-far blocks only; inert blocks frozen exactly
  by y := q0. ANELoopTests + all DYAD suites green.

## 5. THE DEVICE CAPTURE — dissection (the blue face)

First on-device export of the new laws (1.1 MB, 64 frames,
per-frame LCTs, full provenance):

    DYAD MIXTURE phases=2 muF=0.876 muB=0.245 piB=0.524
                 σ=0.160 s*=0.564 τ=0.0407 m*=0.3925m α=0.952
    PHASE F v1  D16=0.00451 D32=0.00561 D64=0.00705 F=0.0172
                u4=1182.9 u2=286.5 u1=379.7 gap=0
                logW=209172 blocks=2640

- PP1 holds on device: gap = 0. The telemetry pipeline works.
- **THE FINDING: frame 0 has ZERO primary-half pixels** (0/65536).
  The face renders through the σ-mirror — its pixel hue ≈ 230°,
  skin's exact opposite. The fitted "face" centroid is gray-green
  (C=0.022, h≈94°): the ORDER phase captured the car interior or
  nothing, not the subject. u4 ≫ u2,u1 confirms: massive DC error.
- Mechanism: the F-labeled cluster (s≈0.876, 47.6% mass) received
  no primary pixels, which is only consistent with the ROLE LABELS
  RUNNING BACKWARDS on this capture — the subject sat at LOW s, so
  the posterior pushed it into the chaos phase (crossover reported
  at 0.39 m — hoodie distance). The mixture's F/B naming convention
  assumes near = high s; DM4 (affine covariance) deliberately
  covers increasing maps only, so a decreasing signal orientation
  silently swaps phases.
- "It lacks color" decomposes into three factors, all downstream of
  the swap: (1) subject hue-mirrored; (2) primaries fit to a gray
  scene (meanC 0.039); (3) the scene-fit ground law then mutes the
  σ half to those gray moments (meanC 0.020) — professional,
  correctly muted background law applied to the WRONG occupant.

## 6. Next session (owed)

1. **ORIENTATION GUARD (the fix):** the role labels must be anchored
   by subject evidence, not the near-=-high-s convention — e.g.
   label F = the cluster holding the face-region depth mass (LIVE:
   center-weighted; FACE: mesh mask), with a DM axiom making labels
   orientation-free (covariant under DECREASING maps too). Verify
   the raw signal orientation on this device path (DEBUG probe).
2. Device flag-flip of phaseChaosLoop + A-series microbenchmark
   (L2/L3 exit) — meaningful only after the orientation fix.
3. Rulings: D, F, B+E live-loop wiring, chaos-flag surface.
4. PHASE F baselines on a correctly-oriented capture.

Suites at sunset: spec 38/38; Swift DYAD/phase/loop suites green;
compile clean. All of it in this commit.
