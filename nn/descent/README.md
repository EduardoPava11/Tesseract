# nn/descent — RETIRED TO LESSONS (2026-08-12)

Daniel's ruling: "WHY do you keep making different paths?
DescentAssign vs DyadAssign... we are working on a model that
requires MLX JEPA-H then port to iPhone... for performance.
I want beauty."

This lab drifted into a SECOND assignment path duplicating
DyadAssign's job. It is retired. What survives is what became LAW
and what the one true model line inherits:

- **DL6 conditional exactness** and **DL7 lookahead dominance**
  (spec/neural/DescentLadder.hs, suite-green): lookahead-L2 is a
  lawful ALGORITHM upgrade to assignment — if it ever ships, it
  ships inside the existing DyadAssign/CPU path, never as a
  separate artifact.
- The corpus emitter + self-gating pattern and the residual-on-
  spine training recipe carry to nn/jepa.
- DescentAssign.mlpackage is NOT a roadmap item. Do not extend
  this lab. The model line is nn/jepa — ONE model.

Original results preserved in RESULTS.md for the record.
