# nn/descent — the assignment-descent lab (J2)
# Corpus comes from the spec's self-gating emitter:
#   cd ../../spec && make corpus-descent

train:
	python3 train.py

corpus:
	cd ../../spec && make corpus-descent
